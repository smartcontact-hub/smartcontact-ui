# SPA fallback for static hosts that read _redirects (Netlify, Cloudflare Pages…).
# Same intent as the [[redirects]] block in netlify.toml — kept here so other
# static hosts work out of the box.
/*  /index.html  200
