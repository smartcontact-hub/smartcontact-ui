import type { ScrollAreaDesignTokens } from '@primeuix/themes/types/scrollarea';

 export default {
    root: {
        focusRing: {
            style: "solid"
        }
    },
    scrollbar: {
        transitionDuration: "{transition.duration}"
    }
} satisfies ScrollAreaDesignTokens;