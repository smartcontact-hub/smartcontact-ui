import type { GalleryDesignTokens } from '@primeuix/themes/types/gallery';

 export default {
    item: {
        transitionDuration: "{transition.duration}"
    },
    action: {
        transitionDuration: "{transition.duration}"
    },
    navigation: {
        transitionDuration: "{transition.duration}"
    },
    thumbnail: {
        transitionDuration: "{transition.duration}"
    }
} satisfies GalleryDesignTokens;