import type { RippleDesignTokens } from '@primeuix/themes/types/ripple';

 export default {
    root: {
        background: "light-dark(rgba(0, 0, 0, 0.1), rgba(255, 255, 255, 0.4))"
    }
} satisfies RippleDesignTokens;